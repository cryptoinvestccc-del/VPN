import { Disclosure } from '../components/Disclosure'

const qa: { q: string; a: React.ReactNode }[] = [
  {
    q: 'Это замена WireGuard или надстройка над ним?',
    a: (
      <>
        Надстройка. WireGuard шифрует трафик ровно так же, как без нас, а Besy добавляет
        сверху второй слой, который прячет форму пакетов. Если убрать Besy, туннель
        останется рабочим WireGuard-туннелем — просто снова узнаваемым.
      </>
    ),
  },
  {
    q: 'Вы видите, какие сайты я открываю?',
    a: (
      <>
        Нет — содержимое зашифровано WireGuard, и ключи от него у нас нет. По умолчанию
        сервер считает только агрегаты по всей сети: сколько сессий, сколько байт, сколько
        неудачных аутентификаций. Разбивку по клиентам оператор должен включить отдельным
        параметром, и это осознанное решение, а не настройка по умолчанию.
      </>
    ),
  },
  {
    q: 'Что увидит провайдер или DPI?',
    a: (
      <>
        В режиме TLS — обычную TLS-сессию на 443-й порт. В режиме UDP — поток UDP-пакетов
        без узнаваемой сигнатуры и без характерного распределения длин. Ни в одном из
        режимов не остаётся постоянного первого байта, по которому WireGuard опознают в
        первую очередь.
      </>
    ),
  },
  {
    q: 'Насколько падает скорость?',
    a: (
      <>
        Внешний слой добавляет nonce, тег аутентификации и паддинг — это несколько десятков
        байт на пакет, и паддинг считается с учётом MTU, чтобы не появлялась фрагментация.
        Режим UDP по накладным расходам близок к чистому WireGuard; TLS дороже, потому что
        сверху ложится TCP.
      </>
    ),
  },
  {
    q: 'Можно поставить на свой сервер?',
    a: (
      <>
        Да, и это основной сценарий. <code>deploy/install.sh server</code> собирает бинарник,
        заводит непривилегированного пользователя, кладёт конфиг с правами 0640 и
        systemd-юнит. Дальше — вписать PSK и адреса и включить сервис.
      </>
    ),
  },
  {
    q: 'Что происходит, когда я отзываю доступ у устройства?',
    a: (
      <>
        Учётная запись помечается отозванной, но из файла не удаляется: так у оператора
        остаётся запись о том, у кого был доступ, и отзыв нельзя молча отменить, вернув
        строку обратно. Остальные устройства при этом продолжают работать — ключи у них
        свои.
      </>
    ),
  },
]

export function Faq() {
  return (
    <section className="section" id="faq">
      <div className="shell faq">
        <div>
          <p className="eyebrow">Вопросы</p>
          <h2>Коротко и по делу</h2>
          <p className="lede">
            Если ответа нет — спросите в issues, ответим там же и добавим сюда.
          </p>
        </div>

        <div>
          {qa.map((item, i) => (
            <Disclosure key={item.q} question={item.q} defaultOpen={i === 0}>
              {item.a}
            </Disclosure>
          ))}
        </div>
      </div>
    </section>
  )
}
