const limits = [
  {
    title: 'Анализ таймингов остаётся',
    body:
      'Паддинг и мусорные пакеты ломают сигнатуру и длины, но не ритм. Наблюдатель, который смотрит достаточно долго и умеет в корреляцию по времени, всё ещё может делать выводы.',
  },
  {
    title: 'PSK общий на всю сеть',
    body:
      'Внешний слой держится на предразделённом ключе. Индивидуальные учётные данные есть, но общий PSK в схеме остаётся, и его компрометация — это компрометация обфускации (не самого WireGuard).',
  },
  {
    title: 'UDP-режим против активных проб',
    body:
      'Молчащий UDP-порт — тоже признак. Если в вашей сети активно простукивают порты, берите TLS-режим: у него на этот случай есть ответ, а у UDP нет.',
  },
]

export function Limits() {
  return (
    <section className="section section--tight">
      <div className="shell">
        <div className="section-head">
          <div className="section-head__text">
            <p className="eyebrow">Чего мы не умеем</p>
            <h2>Границы, которые кодом не закрываются</h2>
            <p className="lede">
              VPN, который обещает анонимность без оговорок, продаёт не технологию.
              Вот что остаётся открытым — чтобы вы решали, подходит ли это вам.
            </p>
          </div>
        </div>

        <div className="limits">
          {limits.map((l) => (
            <article className="limit" key={l.title}>
              <h3>{l.title}</h3>
              <p>{l.body}</p>
            </article>
          ))}
        </div>
      </div>
    </section>
  )
}
